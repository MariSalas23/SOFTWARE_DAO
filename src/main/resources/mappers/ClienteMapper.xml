<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.unisabana.dyas.sampleprj.dao.mybatis.mappers.ClienteMapper">

    <!-- Mapeo de la consulta para obtener los clientes -->
    <select id="consultarClientes" resultMap="ClienteResult">
        SELECT 
            c.nombre,
            c.documento,
            c.telefono,
            c.direccion,
            c.email,
            c.vetado,
            ir.CLIENTES_documento,
            ir.ITEMS_id,
            ir.fechainiciorenta,  -- Usar como cadena de texto directamente
            ir.fechafinrenta,     -- Usar como cadena de texto directamente
            i.id,
            i.nombre,
            i.descripcion,
            i.fechalanzamiento,
            i.tarifaxdia,
            i.formatorenta,
            i.genero,
            ti.id,
            ti.descripcion
        FROM VI_CLIENTES c
        LEFT JOIN VI_ITEMRENTADO ir ON c.documento = ir.CLIENTES_documento
        LEFT JOIN VI_ITEMS i ON ir.ITEMS_id = i.id
        LEFT JOIN VI_TIPOITEM ti ON i.TIPOITEM_id = ti.id;
    </select>

    <!-- Mapeo de la clase Cliente -->
    <resultMap id="ClienteResult" type="Cliente">
        <id property="documento" column="documento"/>
        <result property="nombre" column="nombre"/>
        <result property="telefono" column="telefono"/>
        <result property="direccion" column="direccion"/>
        <result property="email" column="email"/>
        <result property="vetado" column="vetado"/>
        <!-- Colección de ItemsRentados -->
        <collection property="rentados" ofType="ItemRentado" resultMap="ItemRentadoResult"/>
    </resultMap>

    <!-- Mapeo de la clase ItemRentado -->
    <resultMap id="ItemRentadoResult" type="ItemRentado">
        <id property="id" column="ITEMS_id"/>
        <result property="fechainiciorenta" column="fechainiciorenta"/>
        <result property="fechafinrenta" column="fechafinrenta"/>
        <!-- Asociación con la clase Item -->
        <association property="item" javaType="Item" resultMap="ItemResult"/>
    </resultMap>

    <!-- Mapeo de la clase Item -->
    <resultMap id="ItemResult" type="Item">
        <id property="id" column="id"/>
        <result property="nombre" column="nombre"/>
        <result property="descripcion" column="descripcion"/>
        <result property="fechalanzamiento" column="fechalanzamiento"/>
        <result property="tarifaxdia" column="tarifaxdia"/>
        <result property="formatorenta" column="formatorenta"/>
        <result property="genero" column="genero"/>
        <!-- Asociación con la clase TipoItem -->
        <association property="tipo" javaType="TipoItem" resultMap="TipoItemResult"/>
    </resultMap>

    <!-- Mapeo de la clase TipoItem -->
    <resultMap id="TipoItemResult" type="TipoItem">
        <id property="id" column="id"/>
        <result property="descripcion" column="descripcion"/>
    </resultMap>

</mapper>
